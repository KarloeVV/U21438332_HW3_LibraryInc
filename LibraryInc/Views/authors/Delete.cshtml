@model LibraryInc.Models.authors

@{
    Layout = null; // Disables the layout for this view.
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>
</head>
<body>
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>authors</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.name) <!-- Display the label for the 'name' property. -->
            </dt>

            <dd>
                @Html.DisplayFor(model => model.name) <!-- Display the 'name' property. -->
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.surname) <!-- Display the label for the 'surname' property. -->
            </dt>

            <dd>
                @Html.DisplayFor(model => model.surname) <!-- Display the 'surname' property. -->
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken() <!-- Helps prevent cross-site request forgery (CSRF) attacks. -->

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> <!-- Form submission button for deletion. -->
                |
                @Html.ActionLink("Back to List", "Index") <!-- Link to navigate back to the list view. -->
            </div>
        }
    </div>
</body>
</html>
